import React, { FunctionComponent } from 'react';

const DetailsContainer: FunctionComponent = ({ children }) => {
  return <div className="details-container">{children}</div>;
};

export default DetailsContainer;
